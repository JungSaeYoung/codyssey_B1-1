# 쉘 스크립트 처음부터 — `monitor.sh` 한 줄씩 읽기

이 문서는 `monitor.sh`, `report.sh`, `archive_logs.sh` 가 **왜 그렇게 쓰였는지**를 처음 보는 사람 기준으로 설명한다. "이런 게 있구나" 정도만 알아도 되는 부분은 가볍게, 자주 쓰이는 문법은 자세히 다룬다.

---

## 0. 쉘 스크립트가 뭐냐

쉘 스크립트는 **터미널에 한 줄씩 치던 명령어를 파일에 모아둔 것**이다. 그뿐이다.

```bash
echo "hello"
ls /tmp
date
```

이걸 `hello.sh` 라는 파일에 저장한 뒤 `bash hello.sh` 라고 실행하면, 위 세 줄을 순서대로 터미널에 친 것과 똑같이 동작한다. 변수, 반복문, 조건문 같은 건 그 위에 얹는 편의 기능이라고 보면 된다.

---

## 1. 맨 위 두 줄

```bash
#!/usr/bin/env bash
set -u
```

### `#!/usr/bin/env bash` (셔뱅, shebang)

- 파일 **첫 줄**에 `#!` 로 시작하는 건 OS한테 "이 파일을 어떤 프로그램으로 실행해야 하는지" 알려주는 표지판.
- `/usr/bin/env bash` 는 "지금 PATH에 잡힌 bash 를 실행해줘" 라는 뜻. 그냥 `#!/bin/bash` 도 가능하지만, env 를 쓰는 게 시스템 간 호환성이 좋아 관례처럼 쓴다.
- 이 줄이 있고 파일에 실행 권한(`chmod +x`)이 있으면 `./monitor.sh` 만으로 실행된다. 없으면 `bash monitor.sh` 로만 실행 가능.

### `set -u`

- bash의 "안전 모드 옵션" 중 하나. `-u` 는 **선언하지 않은 변수를 쓰면 즉시 에러로 죽는다**.
- 예: `echo "$HOMI"` 처럼 오타가 나면 보통은 빈 문자열로 처리하고 그냥 지나가지만, `set -u` 가 켜져 있으면 `unbound variable` 에러로 멈춘다. 디버깅이 훨씬 쉬워진다.
- 자주 같이 쓰는 옵션:
  - `set -e` : 명령어 하나가 실패하면 스크립트 종료. (좋아 보이지만 `if` 조건문 안에서 의도적 실패까지 잡아버려서 주의 필요)
  - `set -o pipefail` : 파이프(`|`)로 이은 명령 중 하나라도 실패하면 전체를 실패로 본다.
- 우리 `monitor.sh` 는 `set -u` 만 켰다. 자원 수집 명령이 일부 실패해도 다음 단계로 넘어갈 수 있게.

---

## 2. 변수 — 정의와 기본값

```bash
APP_NAME="${APP_NAME:-agent_app.py}"
APP_PORT="${AGENT_PORT:-15034}"
LOG_DIR="${AGENT_LOG_DIR:-/var/log/agent-app}"
```

bash 변수는 **`이름=값`** 으로 정의한다. `=` **양옆에 공백이 있으면 안 된다** (이건 정말 자주 실수함).

```bash
NAME=goobeom        # OK
NAME = goobeom      # 에러: "NAME 이라는 명령어가 없다"고 해석함
```

값을 꺼낼 때는 `$NAME` 또는 `${NAME}` 을 쓴다. 중괄호는 안 써도 동작하지만, 변수 뒤에 글자가 붙을 때 모호함을 피하려고 쓴다. (`$NAME_temp` 는 `$NAME_temp` 변수로 해석되지만 `${NAME}_temp` 는 `goobeom_temp` 로 해석된다.)

### `${VAR:-기본값}` 패턴

이게 위 코드의 핵심.

- `${APP_NAME:-agent_app.py}` 는 "**`APP_NAME` 이 비어 있거나 정의 안 되어 있으면 `agent_app.py` 를 써라**" 라는 뜻.
- 덕분에 스크립트를 다음 두 방식 모두로 쓸 수 있다.
  ```bash
  ./monitor.sh                          # APP_NAME 기본값 사용
  APP_NAME=other_app.py ./monitor.sh    # 한 번만 다른 값으로 실행
  ```
- cron 으로 돌릴 때 환경변수를 미리 export 해두지 못하는 경우가 많아서, 이런 기본값 패턴이 거의 필수다.

---

## 3. `$(...)` — 명령어 결과를 변수에 담기

```bash
TS="$(date '+%Y-%m-%d %H:%M:%S')"
APP_PID="$(pgrep -f "${APP_NAME}" | head -n1 || true)"
```

`$(명령어)` 는 **그 명령어를 실행한 결과(표준 출력)** 를 그 자리에 끼워 넣는다. "명령어 치환(command substitution)" 이라고 부른다.

- `TS="$(date '+%Y-%m-%d %H:%M:%S')"` → 지금 시각이 `2026-05-11 12:34:56` 같은 문자열로 `TS` 에 저장됨.
- 옛날 문법으로는 백틱(`` `date ...` ``) 도 같은 일을 하지만, 중첩이 안 되고 가독성이 나빠 요즘은 `$()` 만 쓴다.

### `pgrep -f "${APP_NAME}" | head -n1 || true`

여러 개념이 한 줄에 모여 있다.

- `pgrep -f agent_app.py` : 명령줄 전체에 `agent_app.py` 가 들어간 프로세스의 PID(프로세스 ID)를 출력. `-f` 는 "프로세스 이름만 보지 말고 명령줄 전체를 봐라".
- `|` (파이프) : 앞 명령의 출력을 뒤 명령의 입력으로 흘려보낸다.
- `head -n1` : 입력에서 첫 줄만 출력. → 여러 PID가 잡혀도 첫 번째만 가져온다.
- `|| true` : 앞 명령이 실패해도(예: 일치하는 프로세스 없음) 전체를 성공으로 처리. 안 붙이면 `set -e` 환경에서 여기서 죽어버린다.

`||` 와 짝인 `&&` :
- `A && B` : A 가 성공해야 B 실행
- `A || B` : A 가 실패하면 B 실행

---

## 4. 조건문 `if ... then ... fi`

```bash
if [[ -z "${APP_PID}" ]]; then
    echo "Checking process '${APP_NAME}'... [FAIL]"
    exit 1
fi
echo "Checking process '${APP_NAME}'... [OK] (PID: ${APP_PID})"
```

bash 조건문 골격:

```bash
if [[ 조건 ]]; then
    실행할_명령
elif [[ 다른조건 ]]; then
    ...
else
    ...
fi
```

- 끝은 `fi` (if 의 거꾸로). 어색하지만 외워야 한다.
- 조건은 `[ ... ]` 또는 `[[ ... ]]` 안에 쓴다. **`[[ ]]` 가 더 안전하고 기능도 많다** (변수에 공백/특수문자가 있을 때 따옴표 안 해도 어느 정도 동작). 가능하면 항상 `[[ ]]` 쓰자.
- `[[` 와 변수 사이에 **공백 필수**. `[[-z $X]]` 는 에러.

### 자주 쓰는 조건 연산자

| 연산자        | 의미                              |
| ------------- | --------------------------------- |
| `-z "$x"`     | x 가 **빈 문자열**이면 참         |
| `-n "$x"`     | x 가 **비어있지 않으면** 참       |
| `"$a" = "$b"` | 문자열 같으면 참 (`==` 도 됨)     |
| `"$a" != "$b"`| 문자열 다르면 참                  |
| `$a -eq $b`   | **숫자** 같으면 참 (eq, ne, lt, le, gt, ge) |
| `-f "$p"`     | 파일이 존재하면 참                |
| `-d "$p"`     | 디렉토리가 존재하면 참            |
| `-w "$p"`     | 쓰기 가능하면 참                  |

### `exit 1`

스크립트를 즉시 종료한다. 괄호 안 숫자는 **종료 코드(exit code)**.
- `0` = 성공
- `0 이외` = 실패
- cron, 다른 스크립트, CI 시스템은 이 숫자를 보고 성공/실패를 판단한다. **0과 0 이외**, 이 둘만 구분된다고 봐도 무방.

---

## 5. 포트 확인 부분 뜯어보기

```bash
if command -v ss >/dev/null 2>&1; then
    PORT_OK=$(ss -tlnH 2>/dev/null | awk -v p=":${APP_PORT}" '$4 ~ p {print "Y"; exit}')
else
    PORT_OK=$(netstat -tln 2>/dev/null | awk -v p=":${APP_PORT}" '$4 ~ p {print "Y"; exit}')
fi
```

### `command -v ss`

- "`ss` 라는 명령어가 시스템에 있는지" 확인. 있으면 경로를 출력하고 0(성공), 없으면 0이 아닌 값으로 끝남.
- `>/dev/null 2>&1` 은 "출력과 에러 출력 모두 버려라" — 우리는 **있는지 없는지**만 알면 되니까 출력은 필요 없다.

### 리다이렉션 정리

| 표기      | 뜻                                              |
| --------- | ----------------------------------------------- |
| `> 파일`  | 표준출력(stdout)을 파일에 덮어쓰기              |
| `>> 파일` | 표준출력을 파일에 **이어쓰기**                  |
| `2> 파일` | 표준에러(stderr)를 파일에 쓰기                  |
| `2>&1`    | 표준에러를 **표준출력으로 합치기** (순서 중요!) |
| `&> 파일` | stdout, stderr 둘 다 파일에 (bash 전용)         |
| `< 파일`  | 파일 내용을 표준입력으로 넣기                   |

- `/dev/null` 은 "쓰면 사라지는 휴지통" 같은 특수 파일. 출력을 버릴 때 여기로 보낸다.
- `>/dev/null 2>&1` 는 거의 관용구다. "출력/에러 둘 다 무시".

### `awk` 한 줄짜리

```bash
awk -v p=":15034" '$4 ~ p {print "Y"; exit}'
```

awk 는 **줄 단위로 텍스트를 처리하는 작은 언어**. 처음엔 외계어처럼 보이는데, 패턴이 정해져 있다.

- `'패턴 { 액션 }'` : 입력의 각 줄 중 **패턴**에 맞으면 **액션**을 실행.
- `$1, $2, $3, ...` : 줄을 공백으로 쪼갠 1번째, 2번째, 3번째 ... 컬럼.
- `-v p=":15034"` : awk 내부에 `p` 라는 변수를 만들고 값을 넣어준다. (쉘 변수와 awk 변수는 별개 세상이라 이렇게 주입해야 한다.)
- `$4 ~ p` : 4번째 컬럼이 `p` (정규식)에 매칭되면 참.
- `{print "Y"; exit}` : "Y" 출력하고 awk 종료. — 매칭되는 줄을 하나라도 찾으면 더 안 보고 끝낸다.

`ss -tlnH` 의 출력은 4번째 컬럼이 `Local Address:Port` 형태라, `:15034` 가 거기 들어있는지 보는 것.

---

## 6. CPU/메모리/디스크 측정

```bash
CPU_USAGE="$(top -bn1 | awk -F'[ ,]+' '/Cpu\(s\)/ {for(i=1;i<=NF;i++) if ($i ~ /id$/) {print 100 - $(i-1); exit}}')"
```

이건 좀 빡세 보이지만 풀어보면 단순하다.

- `top -bn1` : top 을 배치(b)모드로 1회(n1) 실행. → 한 장면 스냅샷이 텍스트로 나옴.
- `awk -F'[ ,]+'` : awk의 컬럼 구분자를 "공백 또는 쉼표 1개 이상"으로 지정.
- `/Cpu\(s\)/ { ... }` : `Cpu(s)` 가 들어간 줄에서만 액션 실행. (top의 cpu 통계 줄)
- `for(i=1;i<=NF;i++)` : 모든 컬럼을 순회 (`NF` = 총 컬럼 수).
- `if ($i ~ /id$/)` : 어떤 컬럼이 `id` 로 끝나면 → top 출력에서 idle 비율 옆에 "id" 라벨이 붙는다.
- `print 100 - $(i-1)` : 그 컬럼 **바로 앞 숫자**(idle 값)를 100에서 빼서 사용률 출력.

핵심 아이디어: top은 CPU "쉬는 비율(idle)"을 알려주니까, **100 - idle = 사용률**.

```bash
MEM_USAGE="$(free | awk '/^Mem:/ {printf "%.1f", $3/$2*100}')"
```

- `free` : 메모리 정보를 표로 출력.
- `^Mem:` 으로 시작하는 줄: `$2` = total, `$3` = used.
- `printf "%.1f"` : 소수점 1자리 형식으로 출력.

```bash
DISK_USED="$(df -P / | awk 'NR==2 {gsub("%","",$5); print $5}')"
```

- `df -P /` : 루트 파티션 사용량을 POSIX 형식으로 출력.
- `NR==2` : 2번째 줄(헤더 다음 줄)만 처리. `NR` = "현재 줄 번호".
- `$5` : 사용률 컬럼인데 `23%` 처럼 % 가 붙어있어서, `gsub("%","",$5)` 로 % 를 제거.

---

## 7. 임계값 비교 — bash가 소수 계산이 안 된다

```bash
awk -v v="${CPU_USAGE}" -v t="${CPU_THRESHOLD}" 'BEGIN {exit !(v+0 > t+0)}' \
    && echo "[WARNING] CPU threshold exceeded (${CPU_USAGE}% > ${CPU_THRESHOLD}%)"
```

bash의 `[[ $a -gt $b ]]` 같은 비교는 **정수만** 된다. `25.3 > 20` 같은 소수 비교는 못 한다. 그래서 우회로 awk 를 쓴다.

- `awk 'BEGIN { ... }'` : 입력 없이 BEGIN 블록만 실행.
- `v+0` : 문자열 `"25.3"` 에 0을 더해서 강제로 숫자로 변환.
- `exit !(v > t)` : `v > t` 가 참(1)이면 `!`로 0, awk 종료 코드 0 (성공) → 뒤의 `&&` 가 실행됨 → 경고 출력. 거짓이면 종료 코드 1 → `&&` 가 안 실행됨.

이 패턴 = "**bash에서 소수 비교가 필요할 때 쓰는 표준 트릭**". 외워두면 평생 쓴다.

---

## 8. 파일 쓰기 / 로그 누적

```bash
LOG_LINE="[${TS}] PID:${APP_PID} CPU:${CPU_USAGE}% MEM:${MEM_USAGE}% DISK_USED:${DISK_USED}%"
echo "${LOG_LINE}" >> "${LOG_FILE}"
```

- `>>` 는 "이어쓰기". `>` 였으면 매번 덮어써서 마지막 줄만 남았을 것이다.
- 매분 한 줄씩 쌓이므로 시계열 로그가 된다.

---

## 9. 로그 로테이션 (덜 중요, 일단 흐름만)

```bash
SIZE="$(stat -c %s "${LOG_FILE}")"
if [[ "${SIZE}" -ge "${MAX_LOG_SIZE}" ]]; then
    rm -f "${LOG_FILE}.${MAX_LOG_FILES}"
    for ((i=MAX_LOG_FILES-1; i>=1; i--)); do
        [[ -f "${LOG_FILE}.${i}" ]] && mv "${LOG_FILE}.${i}" "${LOG_FILE}.$((i+1))"
    done
    mv "${LOG_FILE}" "${LOG_FILE}.1"
    : > "${LOG_FILE}"
fi
```

- `stat -c %s 파일` : 파일 크기(바이트).
- `for ((i=...))` : C 스타일 for 반복. **bash 전용 문법**, sh 에선 안 된다.
- 아이디어:
  1. 가장 오래된 `.10` 삭제
  2. `.9 → .10`, `.8 → .9`, ... `.1 → .2` 로 한 칸씩 밀기
  3. 현재 `monitor.log → monitor.log.1` 로 이름 변경
  4. `: > monitor.log` 로 빈 파일 새로 생성. (`:` 는 "아무것도 안 함" 명령. 그 출력을 빈 파일로 리다이렉트해서 파일을 0바이트로 만든다.)

---

## 10. `report.sh` — awk 본격 사용

`monitor.log` 의 각 줄에서 시각/CPU/MEM/DISK를 뽑아 평균·최대·최소를 계산한다.

```awk
if (match($0, /\[[0-9]{4}-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}:[0-9]{2}\]/)) {
    ts = substr($0, RSTART+1, RLENGTH-2)
}
```

- `match(문자열, 정규식)` : 매칭되면 `RSTART`(시작 위치)와 `RLENGTH`(길이)를 세팅. 매칭 안 되면 0.
- `substr(문자열, 시작, 길이)` : 부분 문자열 뽑기.
- `[YYYY-MM-DD HH:MM:SS]` 에서 양옆 대괄호 제외하고 안쪽만 가져온다.

```awk
n++
cpu_sum += cpu
if (n == 1 || cpu > cpu_max) { cpu_max = cpu; cpu_max_ts = ts }
```

- `n++` : n을 1 증가. (awk는 변수 초기화 없이 바로 더해도 0으로 시작한다.)
- 첫 줄이거나 지금까지의 최대보다 크면 최대값 갱신, 그 시각도 기억.

```awk
END {
    printf "Average : %.1f%%\n", cpu_sum/n
}
```

- `END { ... }` : 모든 입력을 읽은 뒤 마지막에 1번 실행.
- `printf` : C 스타일 포맷 출력. `%.1f` = 소수 1자리, `%%` = 리터럴 `%`, `\n` = 개행.

---

## 11. `archive_logs.sh` — `find` 와 안전 루프

```bash
while IFS= read -r -d '' f; do
    ...
done < <(find "${SRC_DIR}" -maxdepth 1 -type f -name '*.log' -mtime +7 -print0 2>/dev/null)
```

낯설어 보이는 건 다 **공백/특수문자가 든 파일명에서도 안전하게 동작하기 위한 관용구**다.

### `find` 옵션

- `-maxdepth 1` : 하위 디렉토리로 내려가지 말고 그 폴더 안만 봐라.
- `-type f` : 일반 파일만.
- `-name '*.log'` : 이름이 `.log` 로 끝나는 것만.
- `-mtime +7` : 마지막 수정으로부터 7일 **초과** 지난 것만. (`-mtime -7` 은 7일 이내)
- `-print0` : 파일명 사이를 줄바꿈이 아니라 **널 문자(`\0`)** 로 구분해서 출력. 이름에 공백/줄바꿈이 들어있어도 안전.

### `while read` 구문

- `IFS=` : "이번 read 에서 단어 구분자(Internal Field Separator)는 없음" → 공백 보존.
- `read -r` : 백슬래시 이스케이프 처리 안 함 (raw).
- `read -d ''` : 입력 구분자를 널 문자로 (find 의 `-print0` 와 짝).
- `done < <(...)` : "프로세스 치환". 명령의 출력을 파일처럼 읽게 해준다.

이 패턴 = "**파일 목록을 안전하게 한 줄씩 처리할 때의 정석**". 외워두자.

```bash
gzip -c "${f}" > "${target}"
```

- `gzip -c` : 원본을 지우지 않고 압축 결과를 표준출력으로.
- `> "${target}"` : 그 출력을 새 `.gz` 파일로 저장.
- 압축이 성공해야 원본 삭제 (`&&` 가 아니라 `if` 로 분기해서 실패 시 원본 보존).

---

## 12. 자주 헷갈리는 포인트 모음

1. **`=` 양옆 공백 금지**. `A=1` (O), `A = 1` (X).
2. **변수는 무조건 큰따옴표로 감싸기**: `"${VAR}"`. 공백/특수문자 든 경로에서 안 그러면 깨진다.
3. **작은따옴표 vs 큰따옴표**:
   - `'...'` : 안의 모든 것을 문자 그대로. `$VAR` 도 그대로 출력.
   - `"..."` : `$VAR` 를 값으로 치환.
4. **숫자 비교 vs 문자열 비교**:
   - 숫자: `$a -gt $b`
   - 문자열: `"$a" = "$b"`
5. **백틱 \` \` 보다 `$(...)` 를 써라**. 가독성, 중첩 모두 우월.
6. **종료 코드 0 = 성공**. 사람 직관과 반대라 처음에 헷갈린다.
7. **`if` 의 조건은 명령의 종료 코드를 본다**: `if grep -q foo file; then ...` 는 grep 이 찾으면 0 반환 → 참.
8. **줄바꿈은 LF**. Windows에서 만든 파일은 CRLF라 bash가 `\r` 까지 명령어 일부로 인식해 깨진다 → `dos2unix` 또는 VS Code 우하단의 `CRLF → LF`.

---

## 13. 직접 실험해 보기 좋은 작은 예제

```bash
# 1) 변수와 기본값
NAME="${1:-friend}"          # $1 = 첫 번째 인자
echo "Hello, $NAME!"

# 실행:
# ./hi.sh           → Hello, friend!
# ./hi.sh goobeom   → Hello, goobeom!
```

```bash
# 2) 조건문 + 파일 검사
if [[ -f /etc/hosts ]]; then
    echo "exists"
else
    echo "missing"
fi
```

```bash
# 3) 반복문
for n in 1 2 3 4 5; do
    echo "count: $n"
done
```

```bash
# 4) 명령어 결과 + 조건
COUNT="$(ls /etc | wc -l)"
if [[ "$COUNT" -gt 50 ]]; then
    echo "many files: $COUNT"
fi
```

이 네 개만 손에 익으면 `monitor.sh` 의 90%는 읽힌다.

---

## 14. 학습 흐름 추천

1. 위 13장의 예제 4개를 직접 만들어서 실행 → 변수/조건/반복 감 잡기.
2. `monitor.sh` 를 위에서부터 한 블록씩 읽으며 **"이 줄을 빼면 어떻게 되지?"** 를 상상해 보기.
3. OrbStack 머신에서 일부러 앱을 끄거나 포트를 막은 뒤 `monitor.sh` 를 돌려서, FAIL 경로가 어떻게 동작하는지 확인.
4. `report.sh` 의 awk 부분을 한 줄씩 주석 처리하면서 출력이 어떻게 변하는지 관찰.
5. 마지막으로 `archive_logs.sh` 의 `find` 옵션을 바꿔보며 "7일 → 1일", "30일 → 5일" 처럼 짧게 만들어 동작 확인.

막히면 **그 시점의 변수 값을 echo로 찍어보는 것**이 디버깅 1순위 도구다.

```bash
echo "DEBUG: CPU_USAGE=[$CPU_USAGE] MEM_USAGE=[$MEM_USAGE]" >&2
```

`>&2` 는 "표준에러로 출력". 로그 파일에 섞이지 않게 화면에만 보인다.
